<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuhaokui.statistics.mapper.CommitMapper">

    <select id="getProjectCommitCounts" resultType="ProjectCommit">
       select p.id, p.name, sum(t1.commit_count) as commitCount from push_event_payloads t1
       left join events e on t1.event_id = e.id
       left join projects p on e.project_id = p.id
       where e.action = 5
       group by p.id, p.name;
    </select>

</mapper>
